import { CartNumberPipePipe } from './cart-number-pipe.pipe';

describe('CartNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CartNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
